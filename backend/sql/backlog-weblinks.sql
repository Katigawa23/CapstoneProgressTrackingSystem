-- Web links are stored in backlog_attachment, not backlog_weblinks.
-- Use attachment_type = 'link' to insert, query, and delete them.
--
-- Example insert query:
--
-- insert into backlog_attachment (
--   id,
--   backlog_item_id,
--   attachment_type,
--   file_name,
--   file_url,
--   file_type,
--   file_size,
--   link_label
-- )
-- values (
--   gen_random_uuid(),
--   '00000000-0000-0000-0000-000000000000',
--   'link',
--   'Reference link',
--   'https://example.com',
--   'text/uri-list',
--   0,
--   'Reference link'
-- )
-- returning id, backlog_item_id, attachment_type, file_url, link_label, uploaded_at;
--
-- Example list query:
--
-- select
--   id,
--   backlog_item_id,
--   file_url as url,
--   link_label as label,
--   uploaded_at
-- from backlog_attachment
-- where backlog_item_id = '00000000-0000-0000-0000-000000000000'
--   and attachment_type = 'link'
-- order by uploaded_at desc;
--
-- Example delete query:
--
-- delete from backlog_attachment
-- where id = '00000000-0000-0000-0000-000000000000'
--   and backlog_item_id = '00000000-0000-0000-0000-000000000000'
--   and attachment_type = 'link'
-- returning id, backlog_item_id, attachment_type, file_url, link_label, uploaded_at;
