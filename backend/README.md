# Backend

This folder contains the Node.js backend for the capstone tracking system.

## Scripts

- `npm run dev` starts the server in watch mode
- `npm start` starts the server normally

## Endpoints

- `GET /` returns a basic status message
- `GET /health` returns a health check response
